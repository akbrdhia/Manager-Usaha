<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/frameLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.fragment.barang.BarangFragment"
    android:paddingHorizontal="24dp">

    <TextView
        android:id="@+id/tv_home"
        style="@style/judul"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/barang"
        android:textColor="@color/black"
        android:layout_marginTop="94dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/search_input_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        app:boxBackgroundMode="outline"
        app:boxBackgroundColor="@android:color/transparent"
        app:boxStrokeColor="@color/black"
        android:background="@drawable/bg_search_outline"
        app:boxStrokeWidth="1dp"
        app:boxCornerRadiusTopStart="12dp"
        app:boxCornerRadiusTopEnd="12dp"
        app:boxCornerRadiusBottomStart="12dp"
        app:boxCornerRadiusBottomEnd="12dp"
        app:startIconDrawable="@drawable/ic_search"
        app:startIconTint="@color/grey_icon"
        app:hintEnabled="false"
        app:endIconMode="clear_text"
        app:endIconTint="@color/grey_icon"
        app:layout_constraintTop_toBottomOf="@+id/tv_home"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/search_edit_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Cari sesuatu..."
            android:textColorHint="@color/grey_hint"
            android:textColor="@color/black"
            android:textSize="16sp"
            android:inputType="text"
            android:imeOptions="actionSearch"
            android:singleLine="true"
            android:background="@android:color/transparent"
            android:paddingTop="16dp"
            android:paddingBottom="16dp"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"/>

    </com.google.android.material.textfield.TextInputLayout>

    <!-- Toolbar Layout -->
    <LinearLayout
        android:id="@+id/toolbar_layout"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        app:layout_constraintTop_toBottomOf="@+id/search_input_layout"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Tambah Barang Button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_add_barang"
            android:layout_width="wrap_content"
            android:layout_height="58dp"
            android:text="Tambah Barang"
            android:textColor="@color/black"
            android:textSize="14sp"
            android:textAllCaps="false"

            app:cornerRadius="12dp"
            app:strokeColor="@color/dark_variant"
            app:strokeWidth="1dp"
            app:backgroundTint="@color/white"

            app:icon="@drawable/ic_plus"
            app:iconTint="@color/black"
            app:iconGravity="textStart"
            app:iconPadding="8dp"
            app:rippleColor="@color/ripple_grey"
            app:shapeAppearanceOverlay="@style/RoundedButtonShape"

            android:paddingStart="16dp"
            android:paddingEnd="16dp"

            android:layout_marginEnd="12dp"/>


        <!-- Kategori Dropdown with Background Wrapper -->
        <FrameLayout
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:layout_marginEnd="8dp"
            android:background="@drawable/bg_search_outline">

            <com.google.android.material.textfield.TextInputLayout
                android:id="@+id/kategori_dropdown_layout"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                style="@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox.ExposedDropdownMenu"
                app:endIconTint="@color/grey_icon">

                <Spinner
                    android:id="@+id/kategori_spinner"
                    android:layout_width="match_parent"
                    android:layout_height="48dp"
                    android:popupBackground="@color/white"/>

            </com.google.android.material.textfield.TextInputLayout>

        </FrameLayout>

        <!-- Stok Dropdown with Background Wrapper -->
        <FrameLayout
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:background="@drawable/bg_search_outline">

            <com.google.android.material.textfield.TextInputLayout
                android:id="@+id/stok_dropdown_layout"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                style="@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox.ExposedDropdownMenu"
                app:endIconTint="@color/grey_icon"
                app:hintEnabled="false">

                <Spinner
                    android:id="@+id/stok_spinner"
                    android:layout_width="match_parent"
                    android:layout_height="48dp"
                    android:popupBackground="@color/white"/>

            </com.google.android.material.textfield.TextInputLayout>

        </FrameLayout>

    </LinearLayout>

    <!-- RecyclerView for Barang List -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_barang"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="16dp"
        android:clipToPadding="false"
        android:paddingBottom="16dp"
        app:layout_constraintTop_toBottomOf="@+id/toolbar_layout"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        tools:listitem="@layout/item_category" />

    <!-- Loading Indicator -->
    <ProgressBar
        android:id="@+id/progress_bar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="@+id/rv_barang"
        app:layout_constraintBottom_toBottomOf="@+id/rv_barang"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Empty State -->
    <LinearLayout
        android:id="@+id/empty_state"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="@+id/rv_barang"
        app:layout_constraintBottom_toBottomOf="@+id/rv_barang"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Tidak ada barang"
            android:textColor="@color/black"
            android:textSize="16sp"
            android:layout_marginBottom="8dp"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Tambahkan barang pertama Anda"
            android:textColor="@color/black"
            android:textSize="14sp"/>

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>